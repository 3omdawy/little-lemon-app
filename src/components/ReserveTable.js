import '../styles/global.css';

function ReserveTable() {
    return (
        <section className='container'>
            <p>Reserve a table</p>
        </section>
    );
}

export default ReserveTable;